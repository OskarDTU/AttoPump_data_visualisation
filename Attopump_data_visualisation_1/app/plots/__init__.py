"""Plot generation modules (Plotly figures for Streamlit).

Modules
-------
- ``plot_generator``       — core figures for single-test views (time
                             series, boxplot, histogram, sweep).
- ``analysis_plots``       — multi-test comparison figures (overlay,
                             global average, EDA, stability cloud, etc.).
- ``bar_comparison_plots`` — bar-vs-bar comparison figures (sweep overlay,
                             constant-freq boxplots, histograms, summary).
"""
