"""AttoPump Data Visualization App Package.

Sub-packages
------------
- ``app.data``  — configuration, I/O helpers, data processing, persistence.
- ``app.plots`` — Plotly figure generation (single-test, multi-test, bar comparison).
- ``app.pages`` — Streamlit page entry points (Explorer, Analysis, Bar Comparison).
"""
